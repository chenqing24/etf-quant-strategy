# 🐟 ETF量化策略改进计划
## 基于"只吃鱼身"原则的深度复盘

> 日期: 2026-05-28  
> 原则: 只吃鱼身，不吃鱼尾

---

## 一、问题诊断

### 1.1 159806案例复盘

| 维度 | 系统评估 | 人工观察 | 矛盾 |
|------|----------|----------|------|
| **价格位置** | 50% (鱼身中部) | 从0.935跌到0.878 | 系统只看绝对位置，忽视趋势方向 |
| **DMA** | 未评估 | 下降通道 | 缺失 |
| **MACD** | 未评估 | 向下 | 缺失 |
| **OBV** | 因子之一 | 流出 | 权重太低 |
| **大盘** | 未评估 | 震荡 | 缺失 |

**核心问题**: 系统是"超跌买入"逻辑，用户期望的是"趋势确认买入"逻辑

---

### 1.2 当前策略评分维度缺失

```
【现有7因子】
ADX(40%) | BB_percent(30%) | SAR_trend(20%) | OBV_diff(10%)
     ↓           ↓              ↓              ↓
只看短期    布林带位置      SAR方向        OBV变化
超跌信号    超买超卖       单周期方向      资金流向

【缺失关键维度】
❌ 趋势阶段位置 (在涨势的哪个阶段？)
❌ 多周期趋势一致性 (短期+中期+长期)
❌ MACD/DMA方向 (中期趋势)
❌ 大盘环境 (系统性风险)
❌ 成交量确认 (真假突破)
```

---

### 1.3 第一性原理分析

```
【投资第一性原理】
不亏钱 > 赚钱
确定性高的机会 > 模糊的机会
只吃鱼身 > 抄底鱼尾

【量化策略第一性原理】
趋势确认 > 超跌反弹
多周期共振 > 单周期信号
风险控制 > 收益最大化
```

---

## 二、改进方向

### 2.1 改进矩阵

| 优先级 | 改进项 | 问题 | 方案 | 预期收益 |
|--------|--------|------|------|----------|
| **P0** | 增加趋势阶段过滤 | 可能在鱼尾买入 | 增加MA20方向过滤 | 高 |
| **P0** | 增加MACD方向过滤 | 忽视中期趋势 | MACD柱必须为正 | 高 |
| **P1** | 增加大盘环境过滤 | 系统性风险未考虑 | 大盘MA5>MA20才买 | 中 |
| **P1** | 调整因子权重 | ADX权重过高 | 降低ADX，增加趋势因子 | 中 |
| **P2** | 增加成交量确认 | 可能假突破 | 量比>1才买 | 中 |
| **P2** | 动态止损 | 固定止损太粗 | 跟踪止盈 | 低 |

---

### 2.2 新增过滤器设计

```
【过滤器A: 趋势方向过滤】
条件: MA5 > MA10 AND MA10 > MA20
含义: 多头排列，上升趋势
权重: 必须满足，否则不买

【过滤器B: MACD方向过滤】
条件: MACD柱 > 0
含义: 中期趋势向上
权重: 必须满足，否则不买

【过滤器C: 大盘环境过滤】
条件: 沪深300 MA5 > MA20
含义: 大盘处于上升趋势
权重: 建议满足，降低仓位

【过滤器D: 位置过滤】
条件: 价格在60日高点40%-80%区间
含义: 不是鱼尾，避免追高
权重: 必须满足，否则不买
```

---

### 2.3 因子权重调整

```
【调整前】                     【调整后】
ADX: 40%                      ADX: 20%
BB_percent: 30%               BB_percent: 15%
SAR_trend: 20%                SAR_trend: 15%
OBV_diff: 10%                 OBV_diff: 10%
                              MACD方向: 20% (新增)
                              趋势一致性: 20% (新增)
```

---

## 三、实施计划

### 3.1 P0级改进 (立即实施)

#### P0-1: 增加趋势方向过滤

```python
def check_trend_direction(df) -> bool:
    """检查趋势方向: MA5 > MA10 > MA20"""
    ma5 = df['close'].rolling(5).mean().iloc[-1]
    ma10 = df['close'].rolling(10).mean().iloc[-1]
    ma20 = df['close'].rolling(20).mean().iloc[-1]
    return ma5 > ma10 > ma20
```

#### P0-2: 增加MACD方向过滤

```python
def check_macd_direction(df) -> bool:
    """检查MACD柱必须为正"""
    ema12 = df['close'].ewm(span=12).mean()
    ema26 = df['close'].ewm(span=26).mean()
    dif = ema12 - ema26
    dea = dif.ewm(span=9).mean()
    macd = dif - dea
    return macd.iloc[-1] > 0
```

#### P0-3: 增加位置过滤

```python
def check_position(df) -> bool:
    """检查价格在合理区间: 40%-80%"""
    high = df['high'].tail(60).max()
    low = df['low'].tail(60).min()
    current = df['close'].iloc[-1]
    position = (current - low) / (high - low) * 100
    return 40 <= position <= 80
```

---

### 3.2 P1级改进 (1-2周)

#### P1-1: 调整因子权重

```python
# 新权重方案
WEIGHTS = {
    'ADX': 0.20,           # 降低权重
    'BB_percent': 0.15,    # 降低权重
    'SAR_trend': 0.15,     # 降低权重
    'OBV_diff': 0.10,      # 保持
    'MACD_dir': 0.20,      # 新增
    'Trend_consistency': 0.20  # 新增
}
```

#### P1-2: 大盘环境过滤

```python
def check_market_environment() -> float:
    """检查大盘环境，返回仓位系数"""
    df_300 = load_market_data('510300')
    ma5 = df_300['close'].rolling(5).mean().iloc[-1]
    ma20 = df_300['close'].rolling(20).mean().iloc[-1]
    
    if ma5 > ma20:
        return 1.0  # 大盘好，满仓
    elif ma5 > ma20 * 0.98:
        return 0.5  # 大盘一般，半仓
    else:
        return 0.2  # 大盘差，轻仓
```

---

### 3.3 验证计划

| 阶段 | 测试内容 | 验收标准 |
|------|----------|----------|
| 回测验证 | 用改进后策略回测159806 | 应该不买 |
| 对比验证 | 改进前 vs 改进后 | 改进后收益更高/回撤更小 |
| 模拟验证 | 模拟交易3个月 | 胜率 > 50% |

---

## 四、预期效果

### 4.1 改进前后对比

| 指标 | 改进前 | 改进后 |
|------|--------|--------|
| **买入信号** | 超跌就买 | 趋势确认才买 |
| **159806** | 买入 | 不买 |
| **预期年化** | ~80% | 待验证 |
| **最大回撤** | ~21% | 待验证 |
| **胜率** | ~60% | 待验证 |

### 4.2 核心改变

```
【改进前】
RSI < 30 → 买入
     ↓
问题: 可能在下跌中抄底

【改进后】
RSI < 30 AND MA5>MA10>MA20 AND MACD>0 → 买入
     ↓
保证: 上升趋势中的超跌反弹（鱼身）
```

---

## 五、风险评估

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| 过滤太严格错过机会 | 中 | 中 | 放宽条件到OR |
| 回测过拟合 | 中 | 高 | 用多个标的验证 |
| 参数敏感性 | 中 | 中 | 稳健参数选择 |

---

## 六、结论

**核心改进**: 从"超跌买入"升级为"趋势确认+超跌买入"

**新增过滤器**:
1. ✅ 趋势方向 (MA5>MA10>MA20)
2. ✅ MACD方向 (MACD柱>0)
3. ✅ 位置过滤 (40%-80%)
4. ⏳ 大盘环境 (MA5>MA20)

**预期收益**:
- 减少"鱼尾"交易
- 提高买入确定性
- 降低最大回撤

---

*文档版本: v1.0 | 创建: 2026-05-28*