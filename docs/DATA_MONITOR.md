# 数据质量监控设计

**日期**: 2026-05-27（初版）| 2026-06-03（v2.0 池对齐）
**状态**: 设计完成 + 动态阈值

---

## 背景

数据质量是量化策略的基石，需要建立监控机制确保数据长期可靠。

**v2.0 变更（2026-06-03）**：
- v9 策略迁移到 15 只核心交易池（2026-06-02 启用）
- 监控阈值从硬编码 50 改为动态读取 v9 池大小
- 详细修复见 `missions/mission-20260602-220742/design_monitor_threshold.md`

---

## 监控目标

1. **数据完整性**：确保 v9 池 15 只 ETF 数据不缺失
2. **数据时效性**：确保数据每日更新
3. **数据正确性**：确保字段逻辑约束满足
4. **异常检测**：及时发现数据异常

---

## 监控指标

### 1. 数据完整性指标

| 指标 | 描述 | 告警阈值 |
|------|------|----------|
| ETF覆盖率 | 数据库ETF数量/目标ETF数量 | <95% |
| 数据行数 | 每只ETF数据行数 | <100行 |
| 时间覆盖 | 数据时间范围 | 缺失超过30天 |
| **交易日完整性（v9 池）** | 当日记录数 vs 配置池 | <15条 → ERROR |
| **缺失比例** | (配置池-实际)/配置池 | >20% → WARNING |

### 2. 数据时效性指标

| 指标 | 描述 | 告警阈值 |
|------|------|----------|
| 数据新鲜度 | 最新入库时间距当前 | >80分钟 |
| 数据延迟 | 最新数据日期距今天数 | >2天 |
| 更新成功率 | 每日更新成功比例 | <90% |

### 3. 数据正确性指标

| 指标 | 描述 | 告警阈值 |
|------|------|----------|
| 字段约束 | high>=close>=low | 任何违规 |
| 价格合理性 | close>0 且 <1000 | 任何异常 |
| 数值连续性 | 日涨幅/跌幅 | >20% |

### 4. 动态阈值机制（v2.0 新增）

| 阈值 | 来源 | 优先级 |
|------|------|--------|
| `min_day_count` | `ETFListLoader().load()` | 下限 10 |
| `expected_etfs` | `ETFListLoader().load()` | 用于完整性检查 |
| `baseline_count` | `get_min_day_count()` | 用于缺失比例计算 |

**重要**：阈值不写死，跟随 `etf_data_live/top500_target_pool.txt` 动态变化。

---

## 监控方案

### 1. 每日定时检查（Cron）

```bash
# 每日收盘后（15:30）执行
30 15 * * 1-5 cd /path/to/etf_strategy && python -m pytest tests/test_data_field_order.py -q
```

### 2. 每周抽样验证

```bash
# 每周一抽取3只ETF进行交叉验证
python scripts/cross_validate_data.py --sample 3 --verbose
```

### 3. 每月全面检查

```bash
# 每月1日进行全面检查
python -m pytest tests/ -q --tb=short
```

---

## 测试用例设计

| 用例 | 验证点 | 执行频率 |
|------|--------|----------|
| `test_数据完整性_ETF数量` | ETF数量>=66 | 每日 |
| `test_数据完整性_数据行数` | 每只ETF>=100行 | 每日 |
| `test_数据时效性_数据新鲜度` | 最新日期<=2天 | 每日 |
| `test_数据正确性_字段约束` | high>=close>=low | 每日 |
| `test_数据正确性_价格合理性` | close在合理范围 | 每日 |
| `test_数据正确性_交叉验证` | 腾讯vs新浪偏差<1% | 每周 |

---

## 告警机制

### 告警级别

| 级别 | 说明 | 触发条件 |
|------|------|----------|
| 🔴 严重 | 数据严重异常 | ETF数量<60 或 数据过期>7天 |
| 🟡 警告 | 数据轻度异常 | ETF数量<65 或 数据过期>2天 |
| 🟢 提示 | 数据需要关注 | 抽样发现异常 |

### 告警方式

1. **钉钉通知**：通过 `qwenpaw channels send` 发送告警
2. **日志记录**：写入 `etf_reports/cache/` 目录
3. **状态文件**：生成 `data_quality_status.json`

---

## 验证结果

### 测试覆盖

| 测试文件 | 测试数 | 覆盖率 |
|----------|--------|--------|
| test_data_field_order.py | 8 | ✅ |
| test_cross_validation.py | 4 | ✅ |
| test_incremental_update.py | 9 | ✅ |
| test_fetcher_coverage.py | 17 | ✅ 90% |
| test_router_coverage.py | 21 | ✅ 80% |
| test_loader_coverage.py | 14 | ✅ 89% |
| **总计** | **81** | **37%** |

---

## 里程碑

| 时间 | 任务 | 状态 |
|------|------|------|
| 08:00 | 数据质量监控设计文档 | ✅ |
| 08:05 | 监控测试用例设计 | 🔄 待实现 |
| 08:10 | 告警机制实现 | 🔄 待实现 |

---

## 下一步

1. 实现监控测试用例（`test_data_monitor.py`）
2. 实现告警通知机制
3. 配置定时任务